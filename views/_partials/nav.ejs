<nav class="navbar navbar-light navbar-soda">
  <a class="navbar-brand" href="#">
    <img src="img/images/logo-v2.svg" width="210" height="44" alt="">
  </a>
  <div class="right-menu">
    <a class="btn btn-primary" href="mailto:hola@casasoda.com" target="_blank">Colaboremos</a>
  </div>
</nav>
  <!--{/* <a
    href={'https://wa.me/' + whatsappNumber}
    target="_blank"
    className="whats-floating"
  >
    <img src={Whats} />
  </a> */}-->
<script>
  $(document).ready(function () {
    // Get the current URL path
    var path = window.location.pathname;

    // Loop through each navbar link
    $('.nav-link').each(function () {
      var href = $(this).attr('href');

      // Check if the link's URL matches the current path
      if (path === href) {
        $(this).addClass('active'); // Add the active class
      }
    });
  });
</script>